<?php

return [
    // ==========================================
    // ОСНОВНЫЕ ПАРАМЕТРЫ
    // ==========================================
    'is_disabled' => false,
    'has_booking' => false,
    'is_edit_mode' => false,
    'default_theme_scheme' => false,
    'disabled_text' => "Магазин временно не работает",
    'main_menu_btn' => 'К магазинам',
    'shop_display_type' => 0,
    'is_product_list' => false,
    'need_hide_disabled_products' => true,
    'shop_coords' => '0,0',
    'pick_up_type' => 0,
    'schedule' => [],
    'map_tiler' => null,
    'can_buy_after_closing' => false,

    // ==========================================
    // СПОСОБЫ ПОЛУЧЕНИЯ
    // ==========================================
    'allow_delivery' => true,
    'allow_pickup' => true,

    // ==========================================
    // 🆕 ДОСТАВКА: ЗОНЫ И НАСТРОЙКИ
    // ==========================================

    // Массив зон доставки
    // Каждая зона: [
    //   'id' => 'uuid',
    //   'name' => 'Название',
    //   'radius' => 5.0, (в км)
    //   'price' => 150.0,
    //   'color' => '#3b82f6',
    //   'is_active' => true,
    //   'created_at' => 'ISO8601',
    //   'updated_at' => 'ISO8601',
    // ]
    'delivery_zones' => [],

    // Сервисы доставки (Яндекс, СДЭК и т.д.)
    'delivery_services' => [],

    // Базовая стоимость доставки (если зоны не настроены)
    'min_base_delivery_price' => 0,

    // Цена за километр (fallback при отсутствии зон)
    'price_per_km' => 0,

    // Коэффициент расстояния (умножается на реальное расстояние)
    'distance_coef' => 1.0,

    // Бесплатная доставка от суммы
    'free_shipping_starts_from' => 0,

    // Минимальная сумма заказа для доставки
    'min_price' => 0,

    // Текст о доставке (отображается в UI)
    'delivery_price_text' => '',

    // Автоматический запрос доставки (интеграция с внешними сервисами)
    'need_automatic_delivery_request' => false,

    // Адрес магазина (для расчёта расстояния)
    'address' => '',

    // Ближайшие города (для подсказок)
    'nearest_cities' => '',

    // ==========================================
    // 🆕 ДОСТАВКА: НАСТРОЙКИ ОТОБРАЖЕНИЯ
    // ==========================================

    // Показывать зоны на карте
    'show_delivery_zones_on_map' => true,

    // Цвет зон по умолчанию (если не указан в зоне)
    'default_zone_color' => '#3b82f6',

    // Максимальное количество зон
    'max_delivery_zones' => 10,

    // Разрешить доставку за пределами зон (с повышенной ценой)
    'allow_delivery_outside_zones' => true,

    // Множитель цены для доставки за пределами зон
    'outside_zones_price_multiplier' => 1.5,

    // Максимальное расстояние доставки (км, 0 = без ограничений)
    'max_delivery_distance' => 0,

    // ==========================================
    // 🆕 ДОСТАВКА: ВАЛИДАЦИЯ
    // ==========================================

    // Минимальный радиус зоны (км)
    'min_zone_radius' => 0.5,

    // Максимальный радиус зоны (км)
    'max_zone_radius' => 100,

    // Проверять пересечение зон при создании
    'validate_zone_overlap' => false,

    // ==========================================
    // ТЕГИ И КАТЕГОРИИ
    // ==========================================
    'venue_tags' => '',

    // ==========================================
    // ОПЛАТА
    // ==========================================
    'payment_info' => '',
    'need_pay_after_call' => false,
    'can_use_cash' => true,
    'can_use_qr' => true,

    // Настройки СБП (Система Быстрых Платежей)
    'sbp_banks' => [
        // 'tinkoff' => [
        //     'enabled' => false,
        //     'terminal_key' => '',
        //     'terminal_password' => '',
        //     'tax' => 'osn', // osn, usn, usn_income, envd, esn, patent
        //     'vat' => 'none', // none, vat0, vat10, vat20, vat110, vat120
        // ],
    ],

    // ==========================================
    // КОРЗИНА
    // ==========================================
    'need_promo_code' => true,
    'need_bonuses_section' => true,
    'need_person_counter' => false,
    'need_health_restrictions' => false,

    // ==========================================
    // КОНТАКТНОЕ ЛИЦО (МЕНЕДЖЕР)
    // ==========================================
    'manager' => [
        'name' => '',
        'phone' => '',
        'email' => '',
        'social_link' => '',
    ],

    // ==========================================
    // 🆕 ГОСТЬ (ДЛЯ НЕАВТОРИЗОВАННЫХ ПОЛЬЗОВАТЕЛЕЙ)
    // ==========================================
    'guests' => [
        // Случайные имена для гостей
        'identities' => [
            'Котик', 'Пёсик', 'Лисичка', 'Зайчик', 'Мишка',
            'Ёжик', 'Белочка', 'Пандочка', 'Совушка', 'Тигрёнок',
        ],

        // Шаблон приветственного сообщения ({name} = имя гостя)
        'welcome_message' => 'Привет, {name}! 👋 Рады видеть тебя в нашем магазине. Если будут вопросы — пиши, мы всегда на связи!',
    ],
];
